{
  "name": "gamepingbooster-web",
  "version": "0.1.8",
  "description": "Minimalist Monochrome Landing Page & Tracking Server for GamePingBooster",
  "scripts": {
    "build": "mkdir -p dist && cp *.html *.css *.js dist/ && cp -r api dist/ && cp -r public dist/ 2>/dev/null || true",
    "dev": "npx serve .",
    "start": "npx serve ."
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
